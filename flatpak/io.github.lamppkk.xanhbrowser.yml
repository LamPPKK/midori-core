app-id: io.github.lamppkk.xanhbrowser
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: xanh-browser
finish-args:
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --device=dri
  - --talk-name=org.freedesktop.GeoClue2
modules:
  - name: xanh-browser
    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DCMAKE_INSTALL_PREFIX=/app
      - -DXANH_ENABLE_ADBLOCK_RUST=ON
      - -DXANH_ADBLOCK_CORE_LIBRARY=/run/build/xanh-browser/flatpak/native/libxanh_adblock_core.so
    sources:
      - type: dir
        path: ..
