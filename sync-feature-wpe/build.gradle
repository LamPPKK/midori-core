plugins {
    id 'com.android.dynamic-feature'
}

android {
    namespace = 'io.github.lamppkk.xanhbrowser.lite.webkitsyncfeature'
    compileSdk = 36

    defaultConfig { minSdk = 31 }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    packaging {
        resources.excludes += '/META-INF/*.version'
    }
}

dependencies {
    implementation project(':app-webkit')
    implementation project(':sync-feature-common')
}
